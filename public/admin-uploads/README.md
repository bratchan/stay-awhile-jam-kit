# Admin Uploads

Images uploaded through the local admin tool are saved here during `npm run dev`.
The admin Save button also writes `admin-state.json` here with character layouts, chat menu setup, cat images, and opening settings.

Commit this folder's image files and `admin-state.json` so they move from computer to computer with the repo.
